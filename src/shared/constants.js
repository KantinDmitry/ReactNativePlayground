const WEEK_DAYS = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'];

const TIME_BUFFER = 2; // buffer after enabling, where alarm can't be launched

const DEFAULT_PLAYLIST = ['E86ujaO5GCM', 'FLCfCQQGAWU', 'lr5RskILpkg'];

export {
    WEEK_DAYS,
    TIME_BUFFER,
    DEFAULT_PLAYLIST,
};
